name: Rust

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install munge
      run: apt-get update && apt-get install munge
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
